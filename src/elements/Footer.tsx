export default function Footer() {

    return (
        <section className="footer">
            <h3>Footer (delete this heading later)</h3>
            <p>Not SUPER designy, but will have all the signatures and social media. Signatures could also simply be stored locally, or in another repository like the agenda, judges, and sponsors.</p>
        </section>
    );
}
