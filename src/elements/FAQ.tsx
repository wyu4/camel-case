export default function FAQ() {

    return (
        <section className="faq">
            <h2>FAQ</h2>
            <p>Pretty much last designy section, but this one could just be hardcoded.</p>
        </section>
    );
}
